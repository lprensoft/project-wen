# 更新日志

记录每个版本对使用者可见的变化，最新的排在最前面。
内部重构、测试与依赖调整不记在这里，它们在提交历史中。

## [v0.6.2] - 2026-08-21

### 新增

- 程序会自己更新自己：设置页「程序维护 · 程序更新」里一键完成下载、校验、替换与重启，Windows / macOS / Linux 都不需要包管理器，也不必再去解压覆盖；后台默认每天查一次新版本，**只查不装**，装不装由你点。按钮上写着的就是下一步会发生的事——不知道有没有新版时它是「检查更新」，查到之后变成「更新到 vX.Y.Z 并重启」
- 命令行新增 `wen update`：不加参数只报告有没有新版与更新说明，`--check` 只查，`-y` 才真的替换程序文件；它不会去重启另一个进程里跑着的服务
- 按日的时间线写成日记的样子：日期、星期几、当天的天气，然后才是那天的正文

### 修复

- 修复角色在调用工具之后接着替你说话的问题：它会编出一句你没说过的话再回答它，一条消息因此换回好几段回复，编出来的「事实」还会被存进长期记忆、被日程照着排表
- 修复后台任务把同一个工具反复提交、直到耗尽单轮上限的问题：日程排表曾连提十三次，两分半烧掉三十万 token。现在同一轮里重复的调用会被当场挡下，反复不收尾的那一轮直接结束
- 修复日程活动的结束轮次出错时，角色不再提起刚做完那件事的问题——经历本来就已经记在表里了
- 程序退出时先停插件再关 HTTP：收到退出信号之后不再有后台任务继续动作

## [v0.6.1] - 2026-08-21

### 新增

- 记忆新增「边界」分类：任何一方明确表达过的不愿意与坚持单独记成一类且不随时间淡忘，角色划下这条线时当场就记；之后再被试探到同一件事，会按上次说过的态度回应，立场真的变了就修订那条记忆、交代改变的来由并把改变演出来——压缩之后、换了会话都还对得上

### 修复

- 修复表里人格分通道时「切回表人格就接不上」的问题：两条通道此前各记各的当前会话，在里人格那条通道上打字会另起一个会话，切回来时又把你从原来的会话拖走、上下文就此断掉；现在分通道的两条通道共用同一个会话，任一条上的 /new 会把两条一起搬到新会话

## [v0.6.0] - 2026-08-21

### 新增

- 新增人物库插件：角色生活里的朋友、家人、常打交道的人各有名字、关系、设定与亲近度，随相处更新；演绎里的熟人只从这里来，新认识的人先登记才算存在，名字不再三天一换
- 新增日程感知插件：角色每天醒来给自己排一张两到四件事的表（留大段空白），到点去做、做完带回一句经历；与你定下的约定单独登记、每轮都在眼前、次日必排进表且不能由它单方面改动，遇到变故会先跟你商量
- 新增健康感知插件：角色偶尔会有日常小恙——淋了雨晚上可能着凉，吃没吃药、去没去医院决定好得快慢；病程随时间自动起落，病着的时候话少没精神。只演绎小毛病，不做疾病模拟器
- 新增人际感知插件：把与你的关系记成一份快照（阶段、彼此称呼、最近的变动、默契与禁区、你的近况），角色对你的态度不再忽远忽近
- 新增心里话插件：角色没说出口的事——真实看法、憋着的话、在等的事——记成有上限的清单，决定态度与潜台词而不被念出来；嘴上说没事，心里可以记三天
- 记忆会「想起来」：每轮按你说的话自动把最相关的一两条记忆正文带到眼前，不再只靠模型主动去翻目录；记忆分类新增「经历」，角色自己做过、遇到过的事有了归处
- 消息通道像人一样收发：你连发几条短消息会合成一轮再回；打开「像人一样发消息」后回复按句分成几条、带打字间隔与输入状态，不再加「(1/N)」前缀；IM 上的对话会被引导成发消息的口吻
- 新增文风观察插件：每轮检查回复里的助手腔（自称 AI、客套收尾、套话、列表加粗、emoji 等）并按天统计，状态命令里有一行汇总，命中会留一条旁注——只测量不干预
- 新增 `wen eval` 命令：按脚本回放一段对话、可强制压缩，跑完由模型按「像不像同一个人、语气是否一致、关系是否连续」打分出报告，用来看改动前后有没有漂
- 天气同时给出今天的概要（早上看过的预报，带不带伞按它来）；明天的预报只在傍晚之后、或你提到「明天」时才出现在角色眼前，几点起看得到可配置

### 变更

- 历史压缩的摘要会保住关系状态、情绪走向、未了的话头与彼此的称呼，并附上角色压缩前的几句原话作为语气锚——压缩之后角色不再只剩设定
- 明天的预报看到三小时后会标「早些时候就知道了」，角色不再把早已看过的预报当新消息反复提醒带伞；叮嘱与提醒说一次就够，成为角色表达的一条通用规则
- 插件中文名对齐：健康感知、人际感知、日程感知；「表里人格」改称「双重人格」
- 按日时间线的注入块改名「最近几天」，与新的「经历」分类区分开

## [v0.5.0] - 2026-08-20

### 新增

- 新增持有物插件：把冰箱、衣柜这类容器里的物品记成一份随演绎增减的账本（买入叠加、消耗减量、用完移除），每件物品自带放入时间，角色能据此判断食材新不新鲜；跨会话与历史压缩都不丢，按人格分库
- 主动开口有了「理由」：插件可以把值得现在说的事投到一块公告板上，心跳会为此提前一拍并带着理由开口，不再无话找话；天气是第一个来源——所在地刚下起雨或雨刚停会成为开口的由头
- 天气同时带上昨天与明天的概要（同一次请求取回，不多打接口）；明天预报有雨会让角色主动提醒带伞，预报撤销则不再提
- 角色扮演新增台词样例：给出几段角色的原话，语气以它为准，样例之外的场合按同一个声音延伸，比形容词式的性格描述更能压住语气漂移
- 表里人格为里人格单独配台词样例，只在里人格接手的轮次注入并声明优先于表人格的样例，不再被表人格的腔调带跑
- 心跳内置一个「生活」情境提示词范例：无人时角色先过自己的日子（更新场景、心情等状态，幅度与实际过去的时间相称），再决定要不要开口
- 暂停心跳时可以顺带指定醒来后的情境（如「睡前暂停到早上，醒来用早安的说法开场」），一次调用完成，不会只做一半
- 消息通道新增「推送后台通知」开关（默认关）：记忆提炼等后台工作留下的说明会推给绑定的用户，此前只有 Web 界面看得到
- 定时任务提示词引导角色在对方随口提到将来的事时顺手定一次性任务，到时主动提起
- 发布流水线支持手动出滚动的开发版预发布；开发版构建的版本号自我标识为「上一个正式版之后第 N 个提交」，不再冒充正式版

### 变更

- 心跳节奏补上自动变快的通道：你一说话，衰减出来的慢节奏就恢复常态（热聊中调快的设定保留）；暂停到点视为睡醒，回到基础节奏；当前节奏每轮亮给模型，便于它在话题正热时调快
- 会话列表按最近活跃排序，很久以前建但一直在用的会话不再沉底
- 持有物插件默认关闭（它依赖同样默认关闭的角色扮演），与现场、心情、身体感知一致
- 侧栏底部的版本号过长时尾部省略，完整版本号在悬停提示里

### 修复

- 修复心跳暂停期间空闲衰减仍在放缓间隔、一觉睡完节奏必然磨到最慢档的问题
- 修复「推送后台通知」会把前台轮次里模型调用失败的报错原文也推到 IM 的问题：失败转译在前门藏起的东西不再从边路漏出去
- 修复记忆自动提炼（定期与压缩前）判据过窄、「我早上十点上班」这类生活近况落不进任何一类的问题，现在与当场保存的判据一致

## [v0.4.1] - 2026-08-20

### 新增

- 微信扫码绑定可以在终端里完成：`wen config plugins` 里选中微信通道会出现操作入口，二维码直接以字符画在终端里（黑白不随终端配色，深色主题也能扫），码下附链接原文兜底，过期自动刷新重绘——远程服务器上不必再为扫码打开 Web UI

### 变更

- 设置页的配置说明文字修订一轮：删掉「（默认）」这类多余标注，把只有开发者才懂的词换成日常说法，补上缺失的说明

### 修复

- 修复终端配置界面（插件、模型列表）按下方向键就整页滚动、第一项被顶出屏幕的问题：列表装得下屏幕时不再滚动

## [v0.4.0] - 2026-08-20

### 新增

- 新增现场状态插件：把角色的所在、穿着、姿态、正在做的事、持续状态与感官焦点记成一份快照随对话注入，跨会话与历史压缩都不丢，场景与穿着不再越聊越漂移
- 身体感知新增即时状态：唤起与疲劳随演绎增减、随时间各按各的速度自然回落，跨会话延续——余韵与恢复有了实体，设置页可调回落速度、可一键重置
- 心跳可以真正睡觉：角色能暂停主动开口一段时间（最长一天），到点按原节奏恢复，你一说话立即恢复
- 心跳情境提示词：可按情境（睡前、闲聊、干活等）配多套唤醒提示词，角色调整节奏时自行切换，消除每次醒来都说同一套的机械感
- 记忆新增按日时间线：每天的对话在次日自动收束成一行「日期 | 关键事件」，最近几天随对话注入，隔几天再聊也接得上「我们最近经历了什么」
- 角色边界表达：角色可以以自己的身份表达不愿意——轻则带开话头，再则说出感受请求停顿，极限处直接拒绝，全部第一人称、从感受出发
- 模型调用失败（如内容被提供商安全策略拦截）时，以当前人格的口吻转成一句走神般的回复，不再把技术错误直接摆进对话；原始错误转入会话注记，随时可查

### 变更

- 隔了很久的心跳唤醒会先回顾记忆与最近经历再决定说什么，不再顺着早已过时的话题接话
- 状态命令的心跳一行会显示当前情境，暂停期间改报恢复时间

### 修复

- 修复部分提供商在回复中途报错时表现为「空回复且正常结束」、什么提示都看不到的问题
- 心跳里被要求保持安静时，不再发来只有动作演绎、一句话都没有的消息
- 界面上没有任何文字的回复轮次现在会显示一行提示，不再完全静默

## [v0.3.0] - 2026-08-19

### 新增

- 新增技能插件：把一批 SKILL.md 放进目录就能被按需读取，常驻上下文的只有名称与用途，正文用到时才取
- 新增飞书与 Lark 两条消息通道：单聊远程会话，长连接收消息，不需要公网地址；两者凭证不通用，可以同时连
- 新增 Telegram 消息通道：私聊远程会话，支持代理
- 表里人格可以各占一条消息通道：在一条通道上说出切换暗号，接手的那一面在另一条通道上回话，心跳与定时任务的结果也跟着当前人格走
- 心跳节奏可以由角色自己在对话里定：聊到兴头上就把下次开口挪近，你说要去忙就往后推

### 变更

- 心跳不再在每轮对话结束后额外调用一次模型来判断节奏，日常聊天少一次开销

### 修复

- 心跳间隔不再出现「16 分 52.5 秒」这样越调越精细的小数，一律取整到分钟
- 心跳间隔顶到设定的上下限时不再反复空转调整
- 两条消息通道绑在同一个会话上时，一边的回复会在另一边重复出现一次

## [v0.2.0] - 2026-08-19

### 破坏性变更

- 此前配置为对外监听（`host: 0.0.0.0`）且没有设置口令的实例，升级后会降级为只监听本机；设置口令后重启即可恢复对外服务

### 新增

- Web UI 加入访问控制：本机访问免口令，从其它机器访问需要口令
- 口令在设置页里设置与修改，容器部署可用 `WEN_AUTH_PASSWORD` 环境变量提供
- 新增 `wen config` 命令：在终端里引导式修改插件与模型配置，远程机器上不打开 Web UI 也能改
- `wen config plugins`、`wen config models` 可以直接进入对应的配置界面
- 服务正在运行时，`wen config` 的改动当场生效；服务没运行时直接改配置文件
- 新增 `wen status`、`wen version`、`wen help` 命令，`wen help <命令>` 查看单条命令的说明

### 变更

- 状态输出压紧版面：模型与思考深度并作一行，上下文窗口并进会话那一行，占用比例就在用量旁边
- 记忆插件的状态行改为提要式的短句

### 修复

- 心跳的倒计时在重启后接着走，不再从头重算——此前重启比心跳间隔更频繁时，心跳一次都不会触发
- 关掉动态心跳后，修改基础间隔不生效，而且此后怎么改都不动
- 天气在重启后沿用上次的观测，不再每次重启都重新抓一遍，也不再有从启动到抓到之间取不到天气的空窗
- 记忆的定期提炼进度在重启后接着攒，不再归零——此前重启比提炼间隔更频繁时，定期提炼一次都不会发生
- 自动压缩期间在设置页改动插件，会让所有对话一起卡住直到本次记忆提炼结束

### 安全

- 补上跨站写请求防护：此前任意网站都能让你的浏览器向本机运行的 Wen Agent 发起写请求

## [v0.1.3] - 2026-08-19

### 新增

- 天气感知插件：按设定的城市定时获取现实天气，作为角色所处环境的一部分每轮注入（需要先开启角色扮演）
- 天气可以分别设置角色所在的城市与自己所在的城市，两人同城时只查一次；奇幻或舞台剧类的设定留空即可
- 天气设置里可以先测试城市能不能查到、当前是什么天气，确认之后再保存
- 模型设置中新增提示词缓存开关（Anthropic 提供商专用，默认开启）：不变的部分命中缓存后按更低的价格计费
- 状态命令会显示最近一轮命中了多少缓存
- 发布页提供 Windows、macOS、Linux 六种平台的现成安装包，不必自己编译

### 变更

- 当前时间、心情、天气这类每轮都在变的信息改在对话末尾给出，离角色开口的位置更近
- 历史对话中间隔较久的地方会标出隔了多长时间
- 压缩后的摘要会记下它对应的时刻，避免其中的时段描述被当成当下
- 记忆的保存判据扩展到日常生活：近况、心情、当下的处境与安排同样会被记住
- 明确说「记住……」时一律保存，不再由模型判断值不值得记
- 长对话的输入费用明显下降：上下文中不变的部分保持稳定，服务端的自动缓存因此能够命中

### 修复

- 聊了多轮之后，角色会把之前对话里出现过的时刻当成现在（深夜聊过，第二天下午仍在演深夜）
- 角色扮演的场景会沿用上文的时段与天色，而不是按当前时间推进
- QQ、微信里回复 /apply 之后，可能先看到执行结果、再看到「已允许」的回执
- 心跳自动调整后的间隔，在插件重新配置或程序退出时可能来不及保存

## [v0.1.2] - 2026-08-18

### 新增

- 对话进行中会定期自动提炼记忆，不必等到历史被压缩才记（默认每 10 轮，可在设置里调整或关闭）
- 提炼到的结论与已有记忆矛盾时，会就地修订那一条，并在正文里交代被推翻的旧说法
- 记忆可以逐步淡忘（默认关闭）：久未提及的记忆先只剩一句要点，更久之后移出记忆库
- 被移出的记忆会留在记忆库的 forgotten 目录里，不会真正删除
- 保存记忆时可以标记它会不会随时间失去意义，只有标记过的才会淡忘
- 后台自动提炼记了什么、改了什么，会实时显示在会话里，且不会被模型看到
- 角色扮演新增「记忆与回想约束」：把生活类记忆标为会淡忘，结论变了就修订旧记忆，不去翻历史对话的原文复述细节，记不清就直说
- 状态命令会显示记忆的提炼节奏与淡忘时限，QQ、微信里同样可见

### 变更

- 记忆索引里的日期显示这条结论最后一次被确认的时间
- 设置里的「单次压缩最多提炼条数」改名为「单次最多提炼条数」，两种提炼场合共用同一个上限

## [v0.1.1] - 2026-08-18

### 新增

- `/status` 会显示心跳的节奏与下次唤醒时间，QQ、微信里同样可见
- 心跳唤醒时会告诉模型距上次聊天过了多久

### 修复

- 心跳会在聊天进行中插话；现在改为对话结束后重新计时

### 变更

- 滚动条改用自定义样式，更细更淡，深浅主题下都不刺眼
- 侧边栏底部去掉分隔线

## [v0.1.0] - 2026-08-18

初始版本。
